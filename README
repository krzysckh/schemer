schemer - wrapper around raylib and chibi-scheme.

dependencies:
  - raylib
  - GNU make

build:
  $ make
  $ make doc
  # make install

(remember to use GNU make on non-gnu systems)

create a new schemer project:
  $ schemer init

run without building:
  $ schemer run

build an executable:
  $ schemer build
