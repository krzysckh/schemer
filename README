schemer - wrapper around raylib and chibi-scheme.

dependencies:
  - raylib
  - GNU make

main idea:

create a new schemer project:
  $ schemer init

run without building:
  $ schemer run

build an executable:
  $ schemer build

read:
  doc/
